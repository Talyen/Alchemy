{
  "framework": "vite",
  "buildCommand": "export ALCHEMY_SKIP_ASSETS=1 && npm run typecheck && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
