# Cursor indexing only — does not affect git or builds.
# Excludes authored media, generated assets, dependencies, and local reports.

Raw Assets/**
src/assets/optimized/**
public/Music/**
dist/**
release-desktop/**
coverage/**
reports/**
node_modules/**
.vite/**
playwright-report/**
test-results/**
