# Audit Decisions Ledger

Durable dispositions from past audit passes. Check this ledger before confirming a candidate; do not re-propose a dispositioned item unless the evidence has changed (new callers, new drift, changed ownership). This is the only file under `docs/Audits/` that records outcomes — run logs, Done tables, and dated status still do not belong in the audit guides.

## How to use

- Before confirming a finding, search this file for the subject path or symbol. A row suppresses only the recorded candidate and remedy, not new evidence or a broader finding that happens to involve the same file.
- When a pass ends with a rejected or deferred proposal, or a borderline candidate is intentionally kept, add one row.
- Keep entries to one line each. Link evidence in the commit or PR, not here.
- Remove a row when its subject is deleted or the disposition no longer applies; note the removal in the commit message.
- Record a concrete revisit condition. Use `never` only for stable architectural or compatibility policy; fast-changing implementation decisions should name the evidence that would invalidate them.

Dispositions: **keep** (intentional under the recorded evidence — do not re-flag until the revisit condition holds), **rejected** (proposal declined — do not re-propose until the revisit condition holds), **deferred** (approved but unscheduled — re-raise only when the user asks or the revisit condition makes it newly urgent). Completed work belongs in git history and current architecture docs, not this ledger.

## Ledger

| Date       | Audit                           | Subject (path / symbol)                                                                   | Disposition | Reason                                                                                                                                                     | Revisit when                                                                 |
| ---------- | ------------------------------- | ----------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 2026-07-29 | ChangeLocalityContextEfficiency | `src/lib/game-data/*` ↔ screens co-edit hotspot                                           | keep        | Content catalogs are SOT; screen co-touch is inherent to authoring, not a missing seam                                                                     | Co-touch expands beyond catalog authoring or produces repeated drift         |
| 2026-07-29 | TypeSafety                      | `run-state-init.ts` `as unknown as` in progress partial helpers                           | keep        | Test/helper keyed partial-apply seam; not on live Zod hydrate paths                                                                                        | The helper reaches live hydrate paths or masks an invalid state              |
| 2026-07-30 | 17-UnitTestAudit                | `tests/lib/balance/damage-peak-diagnostic.test.ts` threshold                              | deferred    | “Over 500” requires a balance-owner decision; current deterministic peak is 196                                                                            | Balance owner selects a new threshold                                        |
| 2026-07-30 | AsyncRace                       | `talent-tree.tsx` delayed `handleUnlock` completion                                       | deferred    | No confirmed lifetime failure; revisit only with reproduced unmount/navigation impact                                                                      | Unmount or navigation impact is reproduced                                   |
| 2026-07-30 | UIInteractionFeedback           | Armory slot/currency keyboard semantics                                                   | deferred    | Requires a product interaction-model decision across multiple drag-oriented surfaces                                                                       | Product interaction model is decided                                         |
| 2026-08-01 | DuplicateFeatureSurface         | `run-loop/screens/*-shop-screen.tsx` adapters                                             | keep        | Shared shop shell removes duplicated chrome; remaining domain adapters do not justify a two-caller generic wrapper                                         | Adapters drift or a third structural twin appears                            |
| 2026-08-01 | StateGravityOwnership           | `run-session-model.ts` `RunSession` projection                                            | keep        | Intentional action-free persistence/read model used by resume codec; replacing it with the aggregate snapshot would widen coupling without removing a seam | Resume no longer needs the projection or coupling materially changes         |
| 2026-08-13 | BehaviorHardening               | Corruption/mystery mid-visit reload persistence                                           | keep        | Mystery visit fields persist on `activeRun` while `currentScreen` is mystery; invalid `eventId` abandons the encounter instead of re-rolling               | Encode/hydrate no longer screen-scopes mystery, or invalid ids re-roll       |
| 2026-08-19 | DuplicateFeatureSurface         | `wildwood-recovery` currentScreen remap                                                   | keep        | Recovery screen deleted; load still remaps `currentScreen` `"wildwood-recovery"` → `"rewards"`. Campfire is the remaining heal-encounter screen.           | Remap fixtures / launch floor no longer need the alias                       |
| 2026-08-03 | InelegantSlop                   | Armory parts targeting overlay / pointer-drag triples                                     | deferred    | Confirmed duplication; deferred pending dedicated armory E2E confidence for drag/targeting regressions                                                     | Armory drag/targeting E2E suite is green and ready for the extract           |
| 2026-08-05 | 11-InelegantSlop                | Stricter Zod on critical active-run fields (drop silent `.catch(0)` for gold/health/deck) | deferred    | Player-data product decision even pre-launch; soft envelope repair remains                                                                                 | Repair policy for critical fields is decided                                 |
| 2026-08-17 | TypeSafety                      | `persisted-battle-state.ts` `as unknown as Partial<BattleState>`                          | keep        | Persistence wire boundary; hydrateBattleState rehydrates piles on resume                                                                                   | Wire schema uses typed card piles without the double cast                    |
| 2026-08-17 | TypeSafety                      | `_field-setter.ts` default `getSlice` `as unknown as`                                     | keep        | Generic Immer helper default; callers pass explicit nested `getSlice`                                                                                      | Default path is used on live hydrate or masks invalid state                  |
| 2026-08-17 | InelegantSlop                   | `subscribeRunSessionCommits` vs `subscribeGameplayCommits`                                | keep        | Feature/storage use the command-boundary name; stores subscribe on the aggregate — not a leftover twin                                                     | Feature code imports `subscribeGameplayCommits` or the wrapper adds behavior |
| 2026-08-17 | ChangeLocalityContextEfficiency | Battle effect-handlers ↔ enemy-turn-utils circular import                                 | keep        | Real madge cycle; extract would break it but co-touch/drift evidence is below the audit bar                                                                | Cycle files repeatedly co-edit or duplicate threshold/effect policy          |
| 2026-08-17 | SideEffectSurface               | Armory `Math.random` salvage/currency/dev-spawn                                           | keep        | Profile-lifetime meta entropy; binding to `shops` would steal shop draws. Homestead salvage materials are granted after the roll, not replayed on resume   | A dedicated `gear` run RNG stream is added to the save schema                |
