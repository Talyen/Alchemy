{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "header-max-length": [2, "always", 120],
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "balance", "perf", "refactor", "test", "chore", "ci", "build", "docs", "style", "revert"]
    ]
  }
}
