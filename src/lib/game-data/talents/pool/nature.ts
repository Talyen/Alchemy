// Talent placeholder definitions for keyword: nature.
import type { TalentDefinition } from "../types";
import { placeholderTalents } from "../types";

export const natureTalents: TalentDefinition[] = [...placeholderTalents("nature", "nature-placeholder", 1, 10)];
