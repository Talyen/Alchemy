// Talent placeholder definitions for keyword: consume.
import type { TalentDefinition } from "../types";
import { placeholderTalents } from "../types";

export const consumeTalents: TalentDefinition[] = [...placeholderTalents("consume", "consume", 1, 10)];
