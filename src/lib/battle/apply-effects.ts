/**
 * Dispatches and reduces card effects to update the battle state.
 * Implementation lives in ./effect-handlers/ (paired with @/lib/game-data/effects).
 */
export { applyCardEffects } from "./effect-handlers";
