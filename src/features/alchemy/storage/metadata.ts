// Save metadata constants used to detect schema compatibility before hydrating localStorage data.
// Depends on no runtime game state so storage IO and defaults can share one source of truth.
export const CURRENT_SAVE_SCHEMA_VERSION = 1;
export const CURRENT_GAME_BUILD_VERSION = "0.1.0";
export const CURRENT_CONTENT_VERSION = 1;
