// Re-exported from validation lib so feature code imports through one path.
export {
  CURRENT_SAVE_SCHEMA_VERSION,
  CURRENT_GAME_BUILD_VERSION,
  CURRENT_CONTENT_VERSION,
} from "@/lib/validation";
