// Random sampling helpers for rewards, shops, routes, and mystery outcomes.
// Re-exports the canonical lib/utils implementation so feature callers keep one barrel.
import { sampleItems } from "@/lib/utils";

export { sampleItems };
