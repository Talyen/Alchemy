// Re-export from unified persistence — kept for backward compat.
// New code should import from "./persistence".
export { PERSISTENCE_CODECS, createDefaultPersistenceFields, encodePersistenceFields } from "./persistence";
